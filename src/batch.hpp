#include <sstream>

#include "lua.hpp"
#include "lib.hpp"
#include "utils.hpp"

int lvldb_batch_tostring(lua_State*);
int lvldb_batch_put(lua_State*);
int lvldb_batch_del(lua_State*);
int lvldb_batch_clear(lua_State*);
